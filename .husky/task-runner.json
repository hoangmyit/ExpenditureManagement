{
   "tasks": [
      {
         "name": "format-csharp-code",
         "command": "dotnet",
         "args": [ "format", "--include", "${staged}" ],
         "group": "pre-push",
         "include": ["**/*.cs"]
      }
   ]
}
